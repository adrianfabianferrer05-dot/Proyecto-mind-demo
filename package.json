{
  "name": "segunda-mente",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Segunda Mente: app personal privada. Sitio estatico + Supabase Edge Functions.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build-check.mjs",
    "verify": "npm run lint && npm run build && npm test"
  },
  "engines": { "node": ">=20" }
}
