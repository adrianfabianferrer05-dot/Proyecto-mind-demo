{
  "name": "segunda-mente",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Segunda Mente: app personal privada. Sitio estatico + Supabase Edge Functions.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node scripts/lint.mjs",
    "check": "node scripts/build-check.mjs",
    "verify": "npm run lint && npm run typecheck && npm run check && npm test && npm run schema && npm run audit:ui",
    "typecheck": "node scripts/typecheck.mjs",
    "audit:ui": "node scripts/ui-audit.mjs",
    "schema": "node scripts/schema-check.mjs",
    "schema:huella": "node scripts/schema-fingerprint.mjs"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
