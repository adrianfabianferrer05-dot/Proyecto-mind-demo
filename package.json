{
  "name": "segunda-mente",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Segunda Mente: app personal privada. Sitio estatico + Supabase Edge Functions.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build-check.mjs",
    "verify": "npm run lint && npm run typecheck && npm run build && npm test && npm run audit:ui",
    "typecheck": "node scripts/typecheck.mjs",
    "audit:ui": "node scripts/ui-audit.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
